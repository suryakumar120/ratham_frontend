import { createChatBotMessage, createClientMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage(`Hello Welcome to the student info system`)],
  widgets: {
    widgetFunc: 
  }

}

export default config